{
  "name": "json-scripts",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "permits": "tsx permit/scrape_permit.ts",
    "pendle": "tsx pendle/pendle.ts",
    "format": "npx prettier .. -w --ignore-path .prettierignore",
    "generate": "tsx generateTokenMap.script.ts",
    "generate:formatted": "npm run pendle && npm run generate && npm run format"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "prettier": "^3.6.2",
    "ts-node": "^10.9.2",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@1delta/chain-registry": "^0.0.3",
    "@1delta/data-sdk": "^0.0.10",
    "@1delta/providers": "^0.0.40",
    "@1delta/wnative": "^0.0.6",
    "viem": "^2.40.0"
  },
  "prettier": {
    "printWidth": 120,
    "semi": false,
    "singleQuote": true
  }
}
